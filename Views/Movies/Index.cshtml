@model MvcMovie.Models.MovieGenreViewModel

@{
    ViewData["Title"] = "Index";
}
<link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet">



<button id="greenButton" class="btn btn-outline-primary">Green Text + Black Background</button>
<button id="fontButton" class="btn btn-outline-primary">Larger Font</button>
<button id="resetButton" class="btn btn-outline-primary">Reset</button>

<hr />
<div id="content">
    <h1>Movie App</h1>

    <a asp-action="Create" class="btn btn-outline-dark">Lisää uusi</a>


    <aside id="infobox" class="alert alert-info" style="width:350px">
        Data ladattu ja päivitetty onnistuneesti!
    </aside>



    <!--SEARCH-->
    <form asp-controller="Movies" asp-action="Index" method="get">
        <p>
            <select asp-for="MovieGenre" asp-items="Model.Genres">
                <option value="" name="optionValue" selected>All</option>
            </select>

            Title: <input type="text" asp-for="SearchString" id="messageInput" />
            <button type="submit" class="btn btn-success btn-sm rounded-1">Search</button>
            <button id="reset" class="btn btn-danger btn-sm rounded-1" type="button">Reset</button>

        </p>
    </form>

    <table class="table" id="movieTable">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Movies![0].Title)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Movies![0].ReleaseDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Movies![0].Genre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Movies![0].Price)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Movies![0].ProductionComp)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Movies![0].Rating)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Movies!)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Title)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ReleaseDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Genre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Price)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ProductionComp)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Rating)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Muokkaa</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Lisätiedot</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Poista</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>




</div>